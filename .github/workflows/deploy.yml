name: Deploy to Vercel

# DISABLED: Relying on Vercel's native Git integration for automatic deployments
# This workflow is kept for reference but will not run automatically
# To re-enable: update VERCEL_TOKEN secret and uncomment the 'on' triggers below

on:
  # workflow_dispatch:  # Disabled - use Vercel Git integration instead
  # push:
  #   branches: [main]

# jobs:
#   deploy:
#     name: Deploy to Vercel
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Install Vercel CLI
#         run: npm install -g vercel@latest
#
#       - name: Deploy to Vercel
#         run: |
#           vercel --prod --yes
#         env:
#           VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
#           VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#           VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}